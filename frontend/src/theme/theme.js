import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#2B9350',
    },
    secondary: {
      main: '#F3F3F3',
    },
    text: {
      primary: '#3c3c3c',
      secondary: '#5C5F6A',
    },
  },
});

export default theme;
